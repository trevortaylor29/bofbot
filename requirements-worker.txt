# Worker (FastAPI; local disk for dev, Cloudflare R2 when R2_* env is set).
# Install: pip install -r requirements-worker.txt
-r requirements.txt
boto3>=1.34.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
