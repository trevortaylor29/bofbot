/**
 * Default production API origin (no trailing slash).
 * Electron main reads this file via `config.cjs`; override with BOFBOT_API_URL / NEXT_PUBLIC_APP_URL in env.
 */
export const API_BASE_URL = "https://bofbot.com";
