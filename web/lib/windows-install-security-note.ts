/** FAQ + download CTAs — Windows SmartScreen / Defender for new publishers. */
export const WINDOWS_SMARTSCREEN_FAQ = {
  q: "Windows shows a security warning when I install?",
  a: 'This is normal for new apps. Click "More info" then "Run anyway". BofBot is safe — it runs entirely on your machine and doesn\'t modify system files.',
} as const;
